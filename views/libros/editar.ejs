<h1>Editar Libro</h1>

<div class="formulario">
    <form action="/libros/<%= libro.id %>/editar" method="POST">
        <div class="campo">
            <label>Título:</label>
            <input type="text" name="titulo" value="<%= libro.titulo %>" required>
        </div>

        <div class="campo">
            <label>Autor:</label>
            <input type="text" name="autor" value="<%= libro.autor %>" required>
        </div>

        <div class="botones">
            <button type="submit">Guardar Cambios</button>
            <a href="/temas/<%= temaId %>/libros">Cancelar</a>
        </div>
    </form>

    <form action="/libros/<%= libro.id %>/borrar" method="POST">
        <button type="submit" class="boton-borrar">Borrar Libro</button>
    </form>
</div>